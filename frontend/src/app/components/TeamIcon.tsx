interface TeamIconInterface {
  name: string;
  logo: string; // Path to logo image from the public folder
}

export default function TeamIcon({ name, logo }: TeamIconInterface) {
  return <div></div>;
}
